# Dart/Flutter build output
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
pubspec.lock

# Platform scaffolding (regenerated by `flutter create` in CI)
android/
ios/
linux/
macos/
windows/
web/

# Signing secrets (provided via CI secrets only)
key.properties
*.jks
*.keystore

# Editor
*.iml
.idea/

# Local flutter-create artifacts (platform is generated in CI)
.metadata
test/
