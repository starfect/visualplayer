{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions: the libmpv playback engine, which has no mobile backend.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["libmpv:default"]
}
